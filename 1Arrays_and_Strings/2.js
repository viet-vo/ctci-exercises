// Check Permutation: Given two strings, write a method to decide if one is a permutation of the other.
// Hash Tables (page 88)

function permu(string1, string2) {
  if (string1.length >= string2.length) {
      baseWord = str
  }
}

module.exports = permu;
